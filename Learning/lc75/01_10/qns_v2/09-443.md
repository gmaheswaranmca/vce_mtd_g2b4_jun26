"read pointer -> find group, write pointer -> store compressed result"

# 443. String Compression

String Compression

## Problem Statement

Given an array of characters `chars`, compress it **in-place** using the following rule:

* Consecutive repeating characters form a group.
* Write the character once.
* If the count is greater than `1`, write the count as separate digits.

Return the **new length** of the compressed array.

---

## Compression Rules

Suppose:

```text
["a","a","a"]
```

Compress to:

```text
["a","3"]
```

---

Suppose:

```text
["a"]
```

Compress to:

```text
["a"]
```

(No count written because count = 1)

---

Suppose:

```text
["a"] repeated 12 times
```

Compress to:

```text
["a","1","2"]
```

Notice:

```text
12

↓

'1'
'2'
```

The digits are stored separately.

---

# Example 1

### Input

```text
chars = ["a","a","b","b","c","c","c"]
```

### Output

```text
Length = 6

chars = ["a","2","b","2","c","3"]
```

### Explanation

Groups:

```text
aa -> a2

bb -> b2

ccc -> c3
```

Compressed:

```text
a2b2c3
```

Length:

```text
6
```

---

# Example 2

### Input

```text
chars = ["a"]
```

### Output

```text
Length = 1

chars = ["a"]
```

Explanation:

```text
Only one character.

Count = 1

Do not write count.
```

---

# Example 3

### Input

```text
chars =
["a","b","b","b","b","b","b","b","b","b","b","b","b"]
```

### Output

```text
["a","b","1","2"]

Length = 4
```

### Explanation

```text
a -> a

bbbbbbbbbbbb -> b12
```

Store digits separately:

```text
b

1

2
```

---

# Constraints

```text
1 <= chars.length <= 2000

chars[i] is:

- lowercase letter
- uppercase letter
- digit
- symbol
```

---

# Observation

Suppose:

```text
chars

a a b b c c c

0 1 2 3 4 5 6
```

You can think of:

```text
read pointer
↓
find group
write pointer
↓
store compressed result
```

Example:

---

Read:

```text
aa
```

Count:

```text
2
```

Write:

```text
a2
```

---

Read:

```text
bb
```

Write:

```text
b2
```

---

Read:

```text
ccc
```

Write:

```text
c3
```

Result:

```text
a2b2c3
```

---

This naturally suggests:

# Two Pointers

```text
read

write
```

---

# Approach 1 (Recommended)

## Read Pointer + Write Pointer

This is the standard in-place solution.

---

### Idea

Use:

```text
read
```

to scan groups.

Use:

```text
write
```

to write compressed result.

---

# Pseudo Code

```text
function compress(chars):
    read = 0
    write = 0
    while read < n:
        current = chars[read]
        count = 0
        while read < n and chars[read] == current:
            read++
            count++
        chars[write] = current
        write++
        if count > 1:
            digits = toString(count)
            for d in digits:
                chars[write] = d
                write++
    return write
```

---

# Dry Run

Input:

```text
a a b b c c c
```

---

Initial

```text
read=0

write=0
```

---

### Group 1

```text
aa
```

Count:

```text
2
```

Write:

```text
a

2
```

Array:

```text
a 2
```

write:

```text
2
```

---

### Group 2

```text
bb
```

Write:

```text
b

2
```

Array:

```text
a 2 b 2
```

---

### Group 3

```text
ccc
```

Write:

```text
c

3
```

Array:

```text
a 2 b 2 c 3
```

Return:

```text
6
```

---

# Another Dry Run

Input:

```text
a a a a a a a a a a a a
```

Count:

```text
12
```

Write:

```text
a

1

2
```

Array:

```text
a 1 2
```

Length:

```text
3
```

---

# Code Explanation

### Part 1

```text
current = chars[read]
```

Start a new group.

---

### Part 2

```text
while chars[read]==current:

    count++
```

Count occurrences.

---

Example

```text
a a a

count=3
```

---

### Part 3

```text
chars[write]=current
```

Write the character once.

---

### Part 4

```text
if count >1
```

Write count.

---

Example:

```text
count=12
```

Convert:

```text
"12"
```

Write:

```text
'1'

'2'
```

separately.

---

# Complexity Analysis

Let:

```text
n = chars.length
```

---

### Time Complexity

Every character:

* read once
* written once

```text
O(n)
```

---

### Space Complexity

```text
O(1)
```

Because:

```text
read

write

count
```

only.

Compression happens in-place.

---

# Pattern Used

## 1. Fast and Slow Pointer (Read / Write) ⭐⭐⭐⭐⭐

This is the most important pattern here.

---

### Template

```text
read scans input


write stores answer
```

---

Generic:

```text
read=0

write=0


while read<n:

    process group

    write result
```

---

Used in:

* String Compression
* Remove Duplicates
* Move Zeroes
* Remove Element
* Array Deduplication
* URL Encoding

---

# Visual Representation

Input

```text
a a a b b c
↑
read
```

Output

```text
a 3 b 2 c
↑
write
```

Read moves ahead.

Write stores compressed output.

---

# Alternative Pattern 2 : Group Start + Group End

Maintain:

```text
start

end
```

---

Example

```text
a a a b b c

S
E
```

Expand:

```text
while chars[end]==chars[start]

    end++
```

Count:

```text
end-start
```

Write compressed result.

Move:

```text
start=end
```

---

## Pseudo Code

```text
start=0

while start<n:

    end=start


    while end<n

    and chars[end]==chars[start]:

        end++


    count=end-start


    write compressed


    start=end
```

---

### Complexity

```text
Time : O(n)

Space: O(1)
```

Same idea.

---

# Alternative Pattern 3 : Build New String

Instead of in-place:

```text
result=""
```

Append:

```text
character

count
```

Finally:

```text
return result
```

---

## Pseudo Code

```text
result=""

for groups:

    result+=char

    if count>1:

        result+=count
```

---

### Complexity

```text
Time : O(n)

Space : O(n)
```

Not allowed by follow-up.

---

# Alternative Pattern 4 : Frequency Map ❌

Some beginners try:

```text
map[char]++
```

Example:

```text
aabccc
```

Map:

```text
a->2

b->1

c->3
```

---

Problem:

You lose order.

Example:

```text
aabbccaa
```

Map:

```text
a->4

b->2

c->2
```

Wrong because:

```text
aa

bb

cc

aa
```

are separate groups.

So:

```text
Frequency Map

❌ Not suitable
```

---

# Alternative Pattern 5 : Run Length Encoding (RLE)

This problem is actually:

```text
Run Length Encoding
```

---

Example

```text
AAAAABBCC
```

Compress:

```text
A5B2C2
```

---

General RLE Pseudo Code

```text
count=1
for i:
    if current==previous:
        count++
    else:
        output previous,count
        count=1
```

---

This is used in:

* PNG compression
* Fax encoding
* Bitmap compression
* Data compression systems

---

# Comparison of Approaches

| Approach                    | Time | Space | In-place | Recommended |
| --------------------------- | ---: | ----: | -------- | ----------- |
| Read / Write Two Pointers   | O(n) |  O(1) | ✅        | ⭐⭐⭐⭐⭐       |
| Start-End Grouping          | O(n) |  O(1) | ✅        | ⭐⭐⭐⭐        |
| Build New String            | O(n) |  O(n) | ❌        | ⭐⭐          |
| Frequency Map               | O(n) |  O(k) | ❌ Wrong  | ⭐           |
| Run Length Encoding Concept | O(n) |  O(1) | ✅        | ⭐⭐⭐⭐        |

---

# Why Two Pointers is Optimal

Suppose:

```text
a a a b b c c c
```

You need:

1. Read groups:

```text
aaa

bb

ccc
```

2. Write:

```text
a3

b2

c3
```

This is exactly:

```text
read pointer

↓

consume group


write pointer

↓

store compressed output
```

No extra array needed.

---

# Interview Takeaway

This problem teaches one of the most common interview patterns:

```text
Read Pointer

+

Write Pointer

↓

Process groups

↓

Write compressed answer
```

Generic template:

```text
while read<n:

    count group

    write character

    write count if needed
```

This is the standard **Fast-Slow Pointer / Read-Write Pointer Pattern**, and it is the expected optimal solution for **LeetCode 443 – String Compression** with:

```text
Time  : O(n)

Space : O(1)
```
