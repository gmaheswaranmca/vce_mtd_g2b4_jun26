# 605. Can Place Flowers

Can Place Flowers

## Problem Statement

You have a flowerbed represented by an integer array `flowerbed`, where:

```text
0 -> empty plot
1 -> already planted flower
```

Flowers cannot be planted in adjacent plots.

Given an integer `n`, return:

```text
true  -> if n new flowers can be planted
false -> otherwise
```

---

# Description

You want to plant exactly `n` flowers.

A flower can be planted at index `i` only if:

```text
flowerbed[i] == 0

AND

left neighbor is empty or does not exist

AND

right neighbor is empty or does not exist
```

That means:

```text
flowerbed[i-1] != 1
flowerbed[i]   == 0
flowerbed[i+1] != 1
```

Boundary positions are handled specially:

* First index has no left neighbor.
* Last index has no right neighbor.

---

# Example 1

### Input

```text
flowerbed = [1,0,0,0,1]

n = 1
```

### Output

```text
true
```

### Explanation

Index:

```text
0 1 2 3 4
1 0 0 0 1
```

Check position `2`:

```text
left  = 0
self  = 0
right = 0
```

Plant:

```text
1 0 1 0 1
```

Planted:

```text
1 flower
```

Since:

```text
1 >= n
```

Return:

```text
true
```

---

# Example 2

### Input

```text
flowerbed = [1,0,0,0,1]

n = 2
```

### Output

```text
false
```

### Explanation

Only index `2` can be planted:

```text
1 0 1 0 1
```

Only:

```text
1 flower
```

Need:

```text
2 flowers
```

Return:

```text
false
```

---

# Example 3

### Input

```text
flowerbed = [0,0,1,0,0]

n = 2
```

### Output

```text
true
```

### Explanation

Initial:

```text
0 0 1 0 0
```

Plant at index 0:

```text
1 0 1 0 0
```

Plant at index 4:

```text
1 0 1 0 1
```

Total:

```text
2 flowers
```

Return:

```text
true
```

---

# Constraints

```text
1 <= flowerbed.length <= 2 * 10^4

flowerbed[i] is 0 or 1

There are no adjacent flowers initially.

0 <= n <= flowerbed.length
```

---

# Observation

Suppose:

```text
flowerbed = [1,0,0,0,1]

n = 1
```

Can we plant at:

```text
index 1 ?
```

No:

```text
1 0 0 0 1
↑

left neighbor is 1
```

---

Can we plant at:

```text
index 2 ?
```

Yes:

```text
1 0 0 0 1

left  = 0

self  = 0

right = 0
```

Plant immediately:

```text
1 0 1 0 1
```

---

This gives an important insight:

> If a flower can be planted now,
> planting it immediately never hurts future choices.

This is a **Greedy Problem**.

---

# Approach 1 (Recommended)

## Greedy Placement

Traverse from left to right.

Whenever:

```text
left empty
current empty
right empty
```
Plant immediately.

Increase:

```text
count++
```

If:

```text
count >= n
```

Return:

```text
true
```

---

# Pseudo Code

```text
function canPlaceFlowers(flowerbed, n):
    count = 0
    for i from 0 to length(flowerbed)-1:
        leftEmpty = (i==0) OR flowerbed[i-1]==0
        rightEmpty = (i==last) OR flowerbed[i+1]==0
        if flowerbed[i]==0 and leftEmpty and rightEmpty:
            flowerbed[i]=1
            count++
            if count >= n:
                return true
    return count >= n
```

---

# Dry Run

Input:

```text
flowerbed=[1,0,0,0,1]

n=1
```

---

### i = 0

```text
flower exists

skip
```

---

### i = 1

```text
left = 1

cannot plant
```

---

### i = 2

```text
left  = 0

self  = 0

right = 0
```

Plant:

```text
1 0 1 0 1
```

Count:

```text
1
```

---

```text
count >= n

1>=1
```

Return:

```text
true
```

---

# Another Dry Run

Input:

```text
flowerbed=[0,0,1,0,0]

n=2
```

---

### i=0

```text
left boundary -> empty

right=0

plant
```

Array:

```text
1 0 1 0 0
```

count=1

---

### i=4

```text
left=0

right boundary

plant
```

Array:

```text
1 0 1 0 1
```

count=2

Return:

```text
true
```

---

# Code Explanation

```text
leftEmpty =

(i==0) OR flowerbed[i-1]==0
```

If:

* first position OR
* left plot empty

then left side is safe.

---

```text
rightEmpty =

(i==last) OR flowerbed[i+1]==0
```

Similarly for right side.

---

```text
if flowerbed[i]==0

and leftEmpty

and rightEmpty
```

This means:

```text
safe to plant
```

---

```text
flowerbed[i]=1
```

Mark planted.

This prevents adjacent future placements.

---

```text
count++
```

Increase planted flowers.

---

```text
if count>=n

return true
```

Early exit optimization.

---

# Complexity Analysis

Let:

```text
m = flowerbed.length
```

### Time Complexity

```text
O(m)
```

Single traversal.

---

### Space Complexity

```text
O(1)
```

Only variables used.

---

# Pattern Used

## 1. Greedy Placement ⭐⭐⭐⭐⭐

This is the standard solution.

---

### Greedy Rule

```text
If a flower can be planted now,

plant it immediately.
```

Why?

Because planting at earliest available position:

* never blocks a better solution
* maximizes future space

---

### Generic Greedy Template

```text
for each position:

    if safe:

        choose it immediately
```

---

This pattern appears in:

* Interval Scheduling
* Jump Game
* Gas Station
* Cookie Assignment
* Activity Selection
* Flowerbed Placement

---

# Alternative Pattern 2 : Count Consecutive Zeros

Instead of modifying the array.

Count blocks of zeros.

Example:

```text
0 0 0 0 0
```

Length:

```text
5
```

Maximum flowers:

```text
(5+1)/2

=3
```

if boundaries are empty.

---

### Example

```text
flowerbed

0 0 0 1 0 0 0 0
```

Zero blocks:

```text
[0 0 0]

can place 2


[0 0 0 0]

can place 2
```

Total:

```text
4 flowers
```

---

## Pseudo Code

```text
count consecutive zeros

for every zero block:

    compute possible flowers

add to total

return total >= n
```

---

### Complexity

```text
Time : O(n)

Space: O(1)
```

More complicated than greedy.

---

# Alternative Pattern 3 : Brute Force Backtracking

Try:

```text
plant

or

don't plant
```

at every index.

---

## Pseudo Code

```text
function solve(index, remaining):

    if remaining==0:

        return true

    if index>=n:

        return false


    option1 = skip


    option2 =

       if safe:

           plant

           recurse


    return option1 OR option2
```

---

### Complexity

```text
O(2^n)
```

Very slow.

Used only for understanding.

---

# Alternative Pattern 4 : DP

State:

```text
dp[i][k]

Can we plant k flowers

using first i positions?
```

Transition:

```text
plant

or

skip
```

---

### Complexity

```text
Time : O(n*k)

Space: O(n*k)
```

Overkill.

---

# Comparison of Approaches

| Approach            |   Time |  Space | Recommended |
| ------------------- | -----: | -----: | ----------- |
| Greedy Placement    |   O(n) |   O(1) | ⭐⭐⭐⭐⭐       |
| Count Zero Blocks   |   O(n) |   O(1) | ⭐⭐⭐⭐        |
| Dynamic Programming | O(n*k) | O(n*k) | ⭐⭐          |
| Backtracking        | O(2^n) |   O(n) | ⭐           |

---

# Why Greedy is Correct

Suppose:

```text
0 0 0 0
```

At first valid position:

```text
1 0 0 0
```

Remaining:

```text
1 0 1 0
```

Total:

```text
2 flowers
```

If you skip:

```text
0 1 0 0
```

Still:

```text
0 1 0 1
```

Total:

```text
2 flowers
```

Greedy never reduces the answer.

Hence:

```text
Always plant when possible.
```

---

# Interview Takeaway

This problem is a classic:

```text
Local safe choice

↓

Choose immediately

↓

Greedy
```

Template:

```text
for every position:

    if left empty
    and current empty
    and right empty:

        plant

        count++
```

This **Greedy Placement Pattern** is the expected optimal solution for **LeetCode 605 – Can Place Flowers** and is one of the most frequently asked easy greedy interview problems.
